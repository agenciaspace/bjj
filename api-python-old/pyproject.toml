[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "bjj-face-api"
version = "2.0.0"
requires-python = ">=3.10"
dependencies = [
    "fastapi==0.115.0",
    "mangum==0.19.0",
]
